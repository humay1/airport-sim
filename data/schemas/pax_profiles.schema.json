{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pax_profiles.schema.json",
  "title": "Passenger profile definition",
  "description": "See spec/04-data-schemas.md 'Phase 0/1 content fields' and spec/08-interfaces-core.md §8.11 (PaxProfileDefinition). WalkSpeedMps: spec/09-interfaces-flow.md §9.6. ShowUpCurve: spec/11-interfaces-schedule.md §11.6. These values are balance content, human-authored (04-data-schemas.md 'Who writes the Phase 1 files'); this schema does not author them.",
  "type": "object",
  "required": ["schema_version", "id", "walk_speed_mps", "show_up_curve"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9_]+$",
      "description": "Stable identifier. Never derived from array position."
    },
    "walk_speed_mps": {
      "type": "string",
      "pattern": "^[0-9]+(\\.[0-9]+)?$",
      "description": "Fixed-point decimal string, must be > 0 (loader-enforced, 08 §8.11)."
    },
    "show_up_curve": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["minutes_before_std", "share_permille"],
        "additionalProperties": false,
        "properties": {
          "minutes_before_std": { "type": "integer", "minimum": 0 },
          "share_permille": { "type": "integer", "minimum": 0 }
        }
      }
    }
  }
}
